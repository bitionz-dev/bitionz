
import Button from '@mui/material/Button';
import styles from "./ApplyButton.module.css"

export default function ApplyButton() {
    return (<Button variant="contained" className={styles.applyButton}>Aplicar</Button>)
}